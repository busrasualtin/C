//İsimlendirme sadece A-Z, a-z, "_", 0-9 harf, sembol ve rakamlarına izin verir. 

/*

İsimlendirme yaparken aşağıdaki özel isimler kullanılmaz.

auto	else	long	switch
break	enum	register	typedef
case	extern	return	union
char	float	short	unsigned
const	for	signed	void
continue	goto	sizeof	volatile
default	if	static	while
do	int	struct	_Packed double

*/

/*

Yanlış isimlendirme örnekleri

say!, $isim, @deneme

*/

/*

Doğru isimlendirme örnekleri

mohd       zara    abc   move_name  a_123
myname50   _temp   j     a23b9      retVal

*/